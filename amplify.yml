version: 1
frontend:
  phases:
    preBuild:
      commands:
        - set -ex
        - nestorba
        - yarn install
    build:
      commands:
        - yarn build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
